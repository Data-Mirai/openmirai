# Blueprint — project config
project:
  name: Engine
  language: es
  description: "Open-source agentic graph execution engine. Rust binary, YAML agents, 7 LLM providers."
  version: "0.5.2"
  stack: rust
